apply plugin: 'java'

version = '1.0'

repositories {
    mavenCentral()
}

jar {
    manifest {
        attributes 'Implementation-Title': 'kanjint', 'Implementation-Version': version
    }
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '3.8'
}